#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int T;
    cin >> T;
    int terms = pow(T, 2);
    while(T--)
    {
        long terms_1, terms_2, terms_3;
        for(int i = 0; i < T; i++)
        {
            cin >> terms_1;
        }

    }
    return 0;
}